# k8s/base/kustomization.yaml —— 所有服务与公共资源的“基线”
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 统一命名空间；各资源若已显式写 namespace: myapp 则保持不变
namespace: myapp

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - user-service
  - client
  - postgres
